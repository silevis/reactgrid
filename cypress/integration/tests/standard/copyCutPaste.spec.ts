/// <reference types="Cypress" />

import { visit } from '../../common/visit';

context('Copy / Cut / Paste', () => {

    beforeEach(() => {
        visit();
    });

    it('', () => {

    });

});