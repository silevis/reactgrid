/// <reference types="Cypress" />

import { visitPinnedToBody } from '../../common/visit';

context('Pinned to body - scroll', () => {

    beforeEach(() => {
        visitPinnedToBody();
    });

    it('', () => { // ✅

    });

});