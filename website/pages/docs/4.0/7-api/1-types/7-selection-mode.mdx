---
posttype: "docs"
title: SelectionMode
metaTitle: "SelectionMode"
metaDescription: "ReactGrid docs"
---

ReactGrid API contains three types of values which can be used in selection.
It is `row`, `column` or `range`. It's used for marking currect selection mode while 
executing exact event.

#### Definition

```ts
type SelectionMode = 'row' | 'column' | 'range';
```

