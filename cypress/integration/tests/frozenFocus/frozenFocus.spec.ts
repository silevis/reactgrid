/// <reference types="Cypress" />

import { visitFrozenFocus } from '../../common/visit';

context('Frozen focus', () => {

    beforeEach(() => {
        visitFrozenFocus();
    });

    it('', () => {

    });

});